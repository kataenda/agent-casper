[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
# rm -rf .next forces a clean Next.js build (no stale incremental output);
# editing this command also changes the Docker build-layer cache key so the
# server can't reuse a previously cached `npm run build` layer.
cmds = ["rm -rf .next && npm run build"]
